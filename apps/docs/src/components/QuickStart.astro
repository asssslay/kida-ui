---
import { getQuickStart } from '../lib/quick-starts'
import CodeBlock from './CodeBlock.astro'

interface Props {
  name: string
}

const { name } = Astro.props
const source = getQuickStart(name)
---

<CodeBlock code={source} lang="tsx" />
